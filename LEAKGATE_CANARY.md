# leak-gate canary 20260810-150249

Single synthetic, publication-safe sample. The CI scan MUST fail on it.
Rotating one class per run: CI does not echo matched content (public logs),
so the pass/fail verdict is the only per-class signal available.

b2-application-key-id: applicationKeyId=EXAMPLE_NOT_REAL
